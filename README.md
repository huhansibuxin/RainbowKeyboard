# RainbowKeyboard

彩虹键盘光效越狱插件。`1.3.0` 把扩散光效的图层改为**复用**（不再每次按键现建约四十层），
并内置度量以证明省了多少。`1.3.1` 修正诊断日志的落盘位置，并新增一行启动自检。
`1.4.0` 把「复用之后光斑会跟着按键滑行」查清（是 Core Animation 的隐式动画）、做成开关，
并顺带把颜色、动画与几何写入一起收尾。
`1.4.1` 修复这个开关关掉后仍然滑行：该键漏登记进跨进程快照。
`1.4.2` 让光效覆盖候选词栏：光效宿主由按键面板容器改为键盘主体（含顶部候选栏）。

## 1.4.2 光效覆盖候选词栏（仍为诊断构建）

**现象**：背景霓虹的光晕铺到键区顶边就停住，键盘最上边那一行（候选词与右侧几个按钮）
始终没有光。

**两处可挂的位置**：键盘自己的视图树里本来就有两个 —— 按键面板容器（只有键区）与
**键盘主体**（同时含顶部候选栏与按键面板）。原先挂的是前者，所以光晕被键区顶边截断。

| 宿主 | 覆盖范围 | 微信键盘的类 |
|---|---|---|
| 按键面板容器 | 只有键区 | `WBKeyboardView` |
| **键盘主体** | 整个键盘（含候选栏） | `WBMainInputView` |

**做法**：宿主改为键盘主体，占用范围即整个键盘，光晕因此可以一直铺到候选词栏。

- 宿主由**精确类钩子**登记，与按键区、键帽、候选容器同属一张注册表：只做一次弱引用
  读取，**不爬 `superview` 祖先链、不递归子树**。
- 原生键盘没有登记键盘主体，因此仍挂在按键区、占用范围与之前完全一致。
- 宿主在使用前重新校验：须在屏、须确实包含键区、尺寸比例须合理。任一不满足即退回
  按键区 —— 不会把光效挂到已经换掉的那个键盘上，也不会拖到屏幕别处。

**其余行为不变**：

- 触摸点仍必须落在键区内，点候选词不会点亮任何键。
- 键框与键面亮灯仍只出现在键位上。
- 键位表在键区坐标系里采集，挂到键盘主体后随之换算到光效视图自己的坐标系；挂载点与
  占用范围一起纳入变更判定，因此键盘主体出现或变化时光效自动跟随，不必重启键盘。

## 1.4.1 开关关掉后仍滑行：该键漏登记进跨进程快照（仍为诊断构建）

**现象**：把「背景霓虹跟随滑行」关掉，光斑照旧在按键之间滑动。

**来源**：这个键没有登记进跨进程快照的位域。键盘扩展受沙盒限制，读不到偏好文件，
也读不到其它域的偏好，渲染参数实际全部由设置进程经快照传给键盘进程。快照是**位置型位域**：
每个键占固定的存在位与数值位，不登记就等于不存在。于是键盘进程看到的是「未设置」，
而开关的语义是「未设置即开启」—— 设置页关成什么都按开启渲染。

**做法**：
- 为该键分配独立位对（存在位 `44`、数值位 `45`），并登记进「由快照提供的键」清单，
  使这个键能被新快照覆盖。
- 位置型位域的两段（存在位 21–30、数值位 31–40）已首尾相接占满，第十一个键无处安放，
  因此新增键一律使用 `44` 位以上的成对空位；已启用的位号不得重排或回收。
- 旧快照里这两位为 `0`，含义是「未设置」，不会被误读成关闭 —— 升级无需清除残留状态。
- 探针按键行新增 `glide=`，记录**渲染端实际解析到的值**（而非设置页保存的值）。
  这两个值在漏登记时恰好相反，可直接用来验证跨进程传递是否成立。

**注意**：快照只在设置页保存时重新发布，升级后需进设置页**触发一次保存**（切换任意一项即可）。

| 项 | 1.4.0 | 1.4.1 |
|---|---|---|
| 设置页保存的值 | `AmbientGlide = 0` | `AmbientGlide = 0` |
| 键盘进程读到的值 | 缺失 → 按默认开启 | `0` |
| 关掉开关后的行为 | 照旧滑行 | 瞬时到位 |

## 实测省下了多少（1.4.0 对比 1.3.1）

纯复用（热）按键的装配耗时，取自实机 `rkperf.log`（同一键盘、同一套参数）：

| 指标 | 1.3.1 | 1.4.0 | 变化 |
|---|---|---|---|
| 热按键中位总计 | 1017us | 413us | **−59%** |
| keys=3 | 840us | 349us | −58% |
| keys=4 | 1006us | 402us | −60% |
| keys=5 | 1088us | 451us | −59% |
| keys=9 | 1515us | 506us | −67% |
| keys=10 | 1115us | 538us | −52% |
| 冷按键中位总计 | 1311us | 1023us | −22% |
| 热按键的图层/路径分配 | 0 / 0 | 0 / 0 | 池化本身在 1.3.0 已完成 |
| 布局门耗时 | 1.75us | 2.12us | 噪声 |

1.4.0 中，颜色段（`col=`）中位 110us、动画提交段（`anim=`）中位 24us，
合计约占装配耗时的 32% —— 这两段是下一轮的靶子。


## 1.4.0 跟随滑行的来源、开关，与同批收尾（仍为诊断构建）

**现象**：复用图层之后，背景霓虹的光斑会在按键之间滑动 —— 按 9 再按 1，光从 9 滑到 1；
更早的版本没有这个动作。

**来源**：Core Animation 对**已经在图层树里**的图层，每次写模型值都会自带一段 0.25 秒的
隐式动画。旧版每次按键都**新建**图层，赋值时没有旧值可动画，所以从不位移；1.3.0 改成复用后，
同一个光晕图层被反复写圆心，于是从上一次的位置动画过来。它同时意味着每次按键要创建数十个
隐式动画对象 —— 在一条本来就被要求「省开销」的路径上。

**做法**：
- 池内**全部图层按属性关闭隐式动画**（`position`/`bounds`/`opacity`/`fillColor`/`path`/
  `colors`/`shadow*` 一律 `NSNull`）。跟随滑行改用**显式** `CABasicAnimation` 实现，
  时长与缓动曲线与原隐式一致，观感不变（快速连打时从**当前表现位置**续动，仍是连贯跟随）。
- 新开关「**背景霓虹跟随滑行**」（默认开启）。关掉即瞬时到位，回到更早版本的观感。
- 颜色：`colorWithAlphaComponent` 改成对同一个 `CGColor` 做 alpha 拷贝；背景光晕的两个光斑
  共用一组基色；渐变键框的两个色标复用同一数组。
- 键框轮廓补 `shadowPath`（原先是每帧离屏重绘，属耗电项，不在主线程耗时里）。
- 只与几何/配置有关的写入（描边宽度、阴影半径、动画曲线与时长）挪出按键路径；键位表改成
  平坦数值数组，两遍扫描不再逐个拆箱，按下键位改用下标比较。
- 配置**真的变了**才释放图层池：`refreshConfigurationIfStale` 每秒最多重查一次，
  无条件释放会让每秒后的第一次按键退化成冷按键。
- 顺带修 1.3.0 探针汇总行的单位（漏乘 `1e6`，此前平均值恒显示 `0.0`），并新增
  `col=`（颜色段）与 `anim=`（动画提交段）分段计时，便于逐项核对。

| 项 | 1.3.1 | 1.4.0 |
|---|---|---|
| 跟随滑行 | 隐式动画的副作用，不可控、不能关 | 显式动画 + 设置开关 |
| 每次按键的颜色对象 | 每键 2 个 `UIColor` + 3 个 alpha 变体 + 5 个 `CGColor` | 每键 2 个 `CGColor` + 2 次 alpha 拷贝 |
| 键缝轮廓阴影 | 每帧离屏重绘整层 | `shadowPath`，无离屏 |
| 每次按键的隐式动画对象 | 数十个 | 0 |

## 1.3.1 诊断日志落盘修正（行为与 1.3.0 相同）

**问题**：1.3.0 的日志路径取自 `NSTemporaryDirectory()`，实机没有落到文件。键盘扩展只被
授予**自己的容器**，而容器 UUID 每次安装或重启都可能变化 —— 写死或只试一个位置都可能落空。

**做法**：
- 运行时依次尝试容器内的若干位置（`$TMPDIR` → 容器 `Documents` → 容器 `Library/Caches`
  → 容器 `tmp` → 系统 `/tmp`），逐个以追加方式打开，**取第一个能写的**，整轮沿用同一路径。
- 新增**启动自检行**：dylib 载入时立即写入首行，记进程号与实际采用路径。按键记录只能在
  一次按键真正走到扩散光效绘制路径之后出现，单凭它分不清「插件没进这个进程」与「进了但
  没触发」；自检行在日志轮转后会自动补回。

## 1.3.0 扩散光效图层复用（含诊断）

**为什么值得做**：扩散光效每次按键都要现建约四十个图层 —— 一个容器、两层环境光底与两个
径向光晕，以及**每个亮起的键四个图层**（键体、轮廓光、渐变、裁切环），每个键还要**现算
贝塞尔路径再拷进两个形状层**。而这套几何与按键无关：键的外框、裁切环、各层 frame 只取决于
该键的矩形，键盘不换键位就不会变。真正每次变化的只有色相、每键透明度峰值、光晕圆心和
动画起始时刻。

**做法**：几何按「脉冲 × 键」建一次并保留，按键只写那些变化的值，动画对象（`CAKeyframeAnimation`）
也保留复用。**打字过程中不再向图层树增删任何图层** —— 被消掉的是每按键的分配与随之而来的
Core Animation 提交，而不是算术。

| 项 | 说明 |
|---|---|
| 脉冲容量 | 与效果上限同容量（默认 2）。池未满时新按键**独占**一个脉冲；池满时复用**最先结束**的那个 —— 正是原版会丢弃的那个，但它继续参与绘制。故同时可见的波数与原版一致 |
| 几何变体 | 按下键用 3pt 环、其它键 2.6pt，frame 与路径差半个点，所以每键存**两套**路径（惰性使用的只有实际用到的那套） |
| 释放时机 | 键位表变化、观感尺寸变化（旋转）、效果样式变化、功能关闭 —— 均为用户节奏的操作；之后首次按键重建 |
| 内存代价 | 池是有界的：2 个脉冲 × 点亮的键 × 4 层。全键盘亮满时约两百层，且只在真正点亮过的键上分配 |
| 未改动 | 观感、两套键盘的其它样式（鲜艳彩虹 / 轻弹）、配置解析、门、注入范围 |

**诊断**（`RKProbe.h`，仅本次诊断构建）：每次按键写一行，含**冷/热分类**、点亮键数、
**新建/复用组数**、**新建图层与路径数**、装配耗时、整条按键路径耗时、布局门耗时；
另每若干次汇总一行平均值，冷热两桶并列 —— 冷按键付的是旧开销，热按键是现在的开销，
一行就能对照出来。

日志写在键盘扩展**自己的沙盒容器**里（`RK_PROBE_ENABLED` 置 0 即整段移除，不留痕迹）。
路径在运行时解析：容器 UUID 每次安装或重启都可能变，而扩展只被授予自己的容器，
所以依次尝试 `$TMPDIR`、容器 `Documents`、容器 `Library/Caches`、容器 `tmp`，
取第一个能写的。文件首行是**启动自检**，在 dylib 载入时即写入：

```
RKPERF boot pid=96124 home=/private/var/mobile/Containers/Data/PluginKitPlugin/<UUID> \
  tmp=/private/var/mobile/Containers/Data/PluginKitPlugin/<UUID>/tmp log=<实际路径>
```

**有自检行** = 插件确实进了这个进程；**只有自检行、没有按键行** = 加载成功但按键没走到
扩散光效路径；**连文件都没有** = 插件没进这个进程（键盘进程是旧的，或注入过滤没命中）。

```sh
# 找文件（容器 UUID 每次都可能不同，永远用 find 定位，不要记死路径）
find /rootfs/var/mobile/Containers/Data/PluginKitPlugin -name rkperf.log
# 看冷热对照的汇总行
grep "RKPERF sum" "$(find /rootfs/var/mobile/Containers/Data/PluginKitPlugin -name rkperf.log | head -1)"
# 看首次启动自检
grep "RKPERF boot" "$(find /rootfs/var/mobile/Containers/Data/PluginKitPlugin -name rkperf.log | head -1)"
```

**如实更正 1.2.0 第 3 项**：键缝遮罩层的两个调用点都被 `preservesBlackFaces` 把门，
而该判断是**常量否**（黑色键面特性早已移除），因此那层 `CAShapeLayer` **从未被创建过** ——
该项实际收益为零，它是死代码而不是每次按键的开销。本版只更正说明，不改变任何行为。

## 1.2.0 基板热路径收尾（观感不变）

回退说明：`1.1.5` 试图把中英/123/符号三个换键面键的闪光改成「切换完成后在新键位闪」，
实机不可靠（中英切换不改变键帽数，判定门根本不触发；123 则两种时序都有），已整条撤销，
重新以 17.10 为基板。本版只做下面六项，不含任何行为改动。

| # | 项 | 改前 | 改后 |
|---|---|---|---|
| 1 | 配置刷新 | 每次按键：1 次加锁 + 跨进程查询 ×2 | 设置页保存时推送；按键路径只剩 1 次浮点比较（另有每秒一次兜底重查） |
| 2 | 参数取值 | 每次按键约 42 次取值 = 84 次字典查找 + 42 次类型探测 | 配置变更时一次解析成扁平数值，绘制路径直接读 |
| 3 | 键缝遮罩层 | 每次按键新建一个 `CAShapeLayer`（路径已缓存） | 复用同一个层 |
| 4 | 布局代际 | 宿主**每次**布局都前移 | 只在**键区尺寸变化**或**有未登记过的键帽登记进来**时前移 —— **1.2.1 已回退，见下节** |
| 5 | 注册表遍历 | `allObjects` 每次都分配一个数组 | 直接枚举，零分配 |
| 6 | 轻弹键帽轮廓 | 每次按下重渲染 + 逐像素提取 | 仅当重新渲染出的位图与上次**逐字节相同**时复用；逐像素只处理能带墨的像素 |

**第 6 项为什么可以缓存而不怕取到过期字形**：缓存的判据是「重新渲染出的位图逐字节相同」，
不是「同一个键」。大小写切换、中英切换都会改变字形像素，因此字节不同就必然重新提取。

## 1.2.1 回退布局代际收窄（1.2.0 复现了 17.9）

**现象**（实机）：九键打字正常；切到英文也正常；**再切回九键，光效的物理命中区域仍是英文那套**
—— 也就是切换过中英之后，键位表最终停在英文布局。

**根因**：1.2.0 把「布局代际」的触发条件收窄成「宿主 bounds 变化」或「有未登记过的键帽登记进来」，
本意是让「不移动任何东西的重布局」不再作废键位表。但微信键盘**两套键面的键帽在切换时都已经登记**
（旧键面被收进隐藏容器但对象仍活着），于是：

- 切回九键 **不会**有新键帽登记 ⇒ 代际不动；
- 注册键帽数**也不动** ⇒ 作为独立兜底的「注册数」判据同样不动。

两个收窄后的判据同时沉默，而键位真的换了 ⇒ `updateKeyFramesForHost:` 判定「没变」，
继续复用英文那套键位表。（去程九键→英文能生效，是因为英文键帽是**第一次**登记。）

**修复**：布局代际恢复为**宿主每次布局都前移**（回到 17.10 的语义）。
「连续打字不重算键位表」这件事本来就由**门**负责（代际/注册数/尺寸三者都没动才复用），
不需要靠收窄代际来实现，所以回退不损失什么。

**其余五项（配置推送式、参数一次解析、遮罩层复用、注册表直接枚举、轻弹轮廓可复用）不受影响。**

## Samsung17.10 候选词变色自行开启

**现象**：没有主动打开候选词变色，但选择「自定义」、或配置回落到内建值之后，
候选词变色变成开启状态并实际生效。

**根因（两处叠加，缺一不可）**：
1. 候选开关的判定语义是 **「键不存在 = 视为开启」**
   （`CandidateGradient.xm` 旧实现 `!prefs[key] || [prefs[key] boolValue]`）。
2. 而「自定义」与回落状态**恰好都不写这三个键**：
   `RKRestoreCustomParameters()` 只恢复 19 个参数键；
   渲染层回落表 `RKPresetSelfUseTable()` 也不含候选键。
   更隐蔽的是 `RKDisplayTransport.h` 的 `RKMergeDisplaySnapshot()` —— 它先
   `removeObjectsForKeys:RKDisplayKeys()` 删掉本地全部显示键，再用 notifyd 快照覆盖；
   快照里没有这一位时，合并结果里**这个键就消失了**，于是被判为「开启」。

**修复**：
- 判定语义反转为 **「必须显式开启」**（键缺失即为关）。
- 设置页三个候选开关的 `default` 从 `<true/>` 改为 `<false/>`，显示与实际生效不再矛盾。
- 主开关做成缓存（`RKCandidateTintMaster`）：关闭时每次文本绘制只读一个标志位，
  不做字典查找；三个绘制钩子本身保留，手动开启时功能照常可用。
- `RKDisplayKeys()` 的位置型位域**一个元素都没动**（删元素会让后续位号前移，
  重演 17.3 那次光效全灭）。

**验证**：`rk_perf/verify_candidate_off.py` —— 从源码抽键表做 7 组仿真：
三个「缺键」场景在旧实现下全部变开（复现现象）、新实现全部保持关；
显式 True 时功能仍可用；键盘扩散的 flag 语义未被波及。

## Samsung17.9 键位布局切换后光效几何不刷新

**现象**：用微信键盘从九键切到全键盘，按一个字母，光效仍从原来九个数字键的位置扩散；
有时甚至完全没有光效。九键本身、以及一开始就在用的那种布局，都是正常的。

**根因**：光效视图的键位表只在 `host.bounds` 变化时才重建（`Tweak.xm` 里
`geometryChanged = !recordedBounds || !CGRectEqualToRect(recordedBounds, host.bounds)`）。
而**切换键位布局不改变键盘区的尺寸** —— 九键和全键盘占用同一块区域，`bounds` 逐字段相同。
于是判断恒为 false，键位表一直停留在做九键时收集的那一套。

扩散原点也不是触点，而是从键位表里找**离触点最近的那个键的中心**
（`RainbowEffectView` 的 `showKeyWaveAtPoint:`）。旧表里没有字母键：触点到最近的
九键大键超过 14pt 就整场不出光效；落在某个大键覆盖范围内时，`origin` 便取那个数字键的
中心 —— 于是看起来“从那九个数字那里扩散”。

**修复**：

- **指纹改为三项**（`RainbowEffectView` 的 `updateKeyFramesForHost:`）：`host.bounds` +
  **host 布局代际**（`RKKeyboardLayoutStamp()`，由 host 的 `layoutSubviews` 钩子递增）+
  **注册键帽数**（`RKRegisteredKeyCount()`）。三项都是 O(1) 读取、零对象分配。换布局必然
  至少改动其中一项；同一布局连续打字三项全不变；既修掉错位，也不把“每次按键都重算几何”
  的开销引回来。
- **淘汰旧布局的键帽**：微信用“把旧布局整块容器隐藏”的方式收纳，键帽自身的 `hidden`
  仍是 NO、`alpha` 仍是 1，原来的自检过不了这一关。`RKKeyViewIsLive()` 改为沿短链上溯
  到 host（2~3 级固定深度，不递归、不匹配类名），链上任何容器隐藏或透明即淘汰；并额外
  要求键帽与 host **同窗口**，排除停在预载窗口里的键帽（其坐标转换会算出貌似合理的数值）。
- **兜底**：严格判定若一个键都留不下，退回宽松判定 —— 丢掉全部键会让光效彻底消失，
  比几何错位更糟。
- **宿主取值同样加校验**：隐藏/离屏的 host 实例不再抢占指针，也不会被当成可用宿主。

**校验**：`rk_perf/verify_layout_switch.py` —— 源码静态一致性 33 项 + 状态机仿真。
仿真复现了旧判据不重算、按下 q 得到九键位置／无光效，并验证新判据重算后扩散原点精确
等于被按下的那个字母键中心（nearest 0.0pt）；另附“同一布局连打 100 次 → 重算 0 次”的
性能回归护栏。

## Samsung17.8 自用预设

- **背景**：注入范围收窄到原生键盘（InputUI）与微信键盘扩展（wxkb_plugin）之后，
  SpringBoard 不再注入，而跨进程状态的重建（relay）原本挂在 SpringBoard 上。结果是
  注销/重启后 notifyd 里的状态为空，键盘进程只能各自去读已保存的 plist；读得到就正常，
  读不到就落到代码里的出厂回落值 —— 那套值接近「鲜艳彩虹」，这就是重启后观感变样的原因。
- **做法（不引入第三个进程）**：把当前这套参数固化成预设「自用」，并让它同时充当
  **渲染层的出厂回落值**。两条可能的结果都通向同一套数字，因此无需重建 relay。
- **单一来源**：`RKPresetSelfUseTable()`（`RKPreferences.h`）。设置页的「自用」预设直接
  引用它，`RainbowEffectView` 的 `number:fallback:low:high:` 也让位于它。两边取值不可能漂移。
- **编号兼容**：「自用」用**新编号 4**，柔和/鲜艳/快速/推荐仍为 0/1/2/3。预设编号会持久化
  进 plist，插入到列表最前面（柔和水波之前）若沿用 0 会让所有已保存的选择改义，故取新号。
  显示顺序由 plist 的 `validValues` 数组决定，与编号无关。
- **取值**：逐项照抄设备 plist 的实时值，保留尾数（如 `0.8033448457717896`），不做取整 ——
  这套预设存在的意义就是复现当前观感。校验脚本 `rk_perf/verify_preset.py` 逐键比对源码表与
  设备 plist，并断言三条不变量：表内每个键都在备份键表里、自用在柔和水波之前、旧编号未位移。
- **设置页显示**：`readPreferenceValue:` 在键缺失时也返回同一张表的值，所以“滑条显示的数”
  与“键盘实际用的数”始终一致。

## Samsung17.7 按键热路径瘦身

审计顺序：先看系统侧有没有能耗裁决 → 再看 dylib 静态规模 → 最后逐项数热路径的对象分配。

- **系统能耗裁决与本插件无关**：`wxkb_plugin.wakeups_resource-*.ips` / `wxkb.*.ips`
  中 `grep -c 'RainbowKeyboard|TweakInject|ellekit'` 均为 **0**，且 heaviest stack
  是 `dyld → Foundation/PlugInKit → UIKitCore → GraphicsServices → CoreFoundation
  → mach_msg_trap` 的标准 runloop 等待路径，`Action taken: none`（只记账未处置）。
  事件时间 09-18，早于本版本安装时间，属微信键盘自身的扩展宿主唤醒。
- **dylib 静态规模可忽略**：2 个 ObjC 类 / 183 selrefs / 30 classrefs，vmsize 128 KB，
  8 条 `LC_LOAD_DYLIB` 全是宿主进程本来就有的框架。dyld 登记成本不是问题。
- **源码扫描无阻塞**：无 `dispatch_sync` / 信号量 / 文件 IO / 目录遍历。
- **真正可消除的是重复分配**，两处：
  1. `CAKeyframeAnimation` 的 `keyTimes` / `values` 与 `CAGradientLayer` 的 `locations`
     全是常量，但旧代码每次按键都重建（含数组里每个 `NSNumber`）。这些数组不可变、
     Core Animation 只读取，改为共享一份。背景霓虹两趟共省 4 组动画常量数组；
     每个参与动画的键再省一组 `fade.keyTimes`。
  2. `keyGutterMask` 的复合路径只取决于 `bounds` 与 `keyFrames`，旧代码每次按键都把
     全部键面（约 32 个圆角贝塞尔路径）重新构建一遍。改为按 `bounds` 缓存，
     `setKeyFrames:` 与 bounds 变化时失效（`dealloc` 释放）。
- **量化（逐项对应源码分配语句的对象账）**：

  | 场景 | reach(pt) | 参与动画键数 N | 改前对象/次 | 改后对象/次 | 降幅 |
  |---|---|---|---|---|---|
  | 出厂默认 180 / 2.0 | 180.0 | 19.4 | 555 | 373 | **-33%** |
  | 设备实测 126.26 / 1.5 | 94.7 | 7.9 | 291 | 166 | **-43%** |
  | 半径小 / 扩散大 126.26 / 2.0 | 126.3 | 12.3 | 391 | 245 | -37% |
  | 全部拉满 360 / 3.0 | 540.0 | 32.0 | 844 | 599 | -29% |

  说明：N 由 `reach = BackgroundRadius × Spread / 2` 决定，所以成本与「最大半径」
  「扩散倍率」两个滑条强相关 —— 当前设备参数已把成本压到出厂默认的约 45%。
- **刻意不改的项（已排除，不再重复怀疑）**：
  - `RKReadEffectivePreferences()` 每次按键有 2 次 `notify_get_state` + 1 次
    `notify_check`（约 30~90 µs）。节流可省，但会把「改设置后下一次按键即生效」
    变成最长 100 ms 延迟，收益与体验不成比例，故保留。
  - `halo` 的 shadow 每层一次离屏合成 —— 那是光晕本身，去掉就是改观感。
  - 轻弹模式的字形截图（`RKPressForeground` 的 render server 取图）是唯一未量化的
    毫秒级项，但仅在该模式下执行；当前设备用的是三星风格扩散，不在此路径上。

## Samsung17.6 自定义参数备份

- 根因：效果预设与「自定义」写的是同一批参数键。预设直接原地覆盖 `Opacity` /
  `Brightness` / `Duration` / `EffectStyle` 等 15 个键，而 `Preset = -1`（自定义）
  本身不承载任何数值，只是一个「被手动调整过」的标记位。因此从预设切回「自定义」
  只会改标签，参数仍是预设的；只有之后再随手拖一次滑条，`Preset` 才会再次变成
  `-1` 并带上新数值 —— 这就是该现象时有时无、看起来像随机失效的原因。
- 修复：预设生效前，先把当前这 15 个参数存入 `CustomParams`；选择「自定义」时整体
  还原。仅当被替换的状态确实是自定义（`Preset == -1`）或从未建立过备份时才写入备份，
  避免预设 A 覆盖预设 B 时把 A 的数值误存成自定义。
- `CustomParams` 刻意不进入 `RKDisplayKeys()`：该显示通道是位置型位域，而它是字典，
  登记进去会让其后所有位号位移，重演 17.3 的错位故障。
- 全新安装后首次选预设会用当前（默认）数值播种备份，保证「自定义」始终有值可还原。
- 首次升级到本版本时，若此前从未建立过备份，需先手动调整一次任意滑条再选「自定义」，
  备份即建立；此后切换预设与自定义均为完整还原。

## Samsung17 原生键盘状态刷新

- 原生 `UIKBKeyplaneView` 的状态切换只刷新实际返回的当前键视图，不再每次状态变化遍历最多 512 个键盘视图。
- 同一轮状态切换中的嵌套回调会合并为一次局部刷新，避免 `setState` 两套接口重复处理同一副键盘。
- 原生按键按下/松开不再触发整副键盘键位几何重算；布局、语言切换和键盘结构变化仍保留完整初始化路径。
- 微信输入法不进入这些原生状态 hook，原有微信输入法效果与性能路径保持不变。
- 新增原生状态性能回归，验证 200 次嵌套状态切换只刷新命中的键，并检查纯黑、自定义颜色和按下/松开恢复。

## Samsung16 输入性能

- 触摸事件只在按下时处理，不再对移动、松开、取消事件重复执行整键盘遍历和遮罩重建。
- 键盘效果视图缓存键盘 bounds、实际键位几何和排除遮罩；尺寸未改变时连续输入复用缓存。
- 创建效果视图后把纯黑键盘重刷移到后续主线程回合，避免在输入事件同步路径上执行大批量图层、图片和状态刷新。
- 轻弹模式优先使用触摸对应的当前键视图，只对该键视图做局部字形提取，不再绘制整副键盘。
- 本机无法直接运行用户的巨魔/越狱 App；已用原生键盘测试宿主、复杂键盘图层和模拟器回归验证，其他插件冲突仍需真机确认。

## Samsung15 纯色灯光

- 轻弹模式使用单个 `CALayer.backgroundColor` 均匀填色，取消渐变、高光和边框亮度差异。保留原有字形保护、短促弹簧、消退、同键去重和层数上限。
- 新增 `PressColorMode`：0 彩色（默认）、1 单色。彩色每次有效点击推进色相，当前单键只用一种饱和纯色，同时亮起的键各自保持颜色；重复点击同键也换色。单色使用 `PressColor` 的 RGB，未设置时为青色 `(0, .8, 1)`。
- `PressBrightness` 范围 0～1，默认 1。按所选 RGB 线性缩放，0 不新建灯光；1 使用完整所选颜色和峰值不透明度，不乘旧的 `Opacity/Brightness/NeonSaturation`。未知复杂皮肤无法提取字形时仍降低覆盖率保护文字。不能超过屏幕显示亮度；单色选得暗时不会擅自改变它。
- 仅轻弹模式使用这三项。原三星扩散、圆环、键帽底色和 QQ 路径不变，不重写用户其他设置。主设置页有 36 个控件、15 份滑条说明，选色复用已验证的系统选色器。
- 跨 App 同步沿用 v2 完整性校验：扩展占用 word1 的 41～44 位、word14 高32位，以及 word2～4 各自高16位。原17个数字、4组颜色、10个开关的位位置不变；旧客户端忽略扩展，新客户端收到不含扩展的完整快照使用默认值。
- 回归检查颜色字段不覆盖旧字段、非法数据拒绝、单色重复点击、彩色逐次换色、多个键不同色、独立亮度、设置保存和独立进程恢复。系统键盘截图另检查单色强光、暗光、彩色多键的均匀像素与字形。仍需 iOS 17.0 真机与微信输入法确认。

## Samsung14 QQ 与性能

- 仅在 `com.tencent.mqq` 且自定义配色开启时，将 `UIKBRenderConfig` 创建接口的 appearance 转成 Dark，避免浅色配置内部状态与后来覆盖的 getter 不一致。分别检查带 `inputMode:` 和带 `traitEnvironment:` 的实际签名；未知 ABI 跳过。保留输入模式、trait environment、聊天页面主题，不在微信输入法或其他 App 安装此 hook。关闭主题、原生键盘或总开关后传回原参数。
- 配置缓存用完整快照 commit 与变更通知失效。未变化时不重复读文件、同步 CFPreferences 或解码全部传输字段；缺失/写入中的快照有有限频率重试，继续保留最后有效值。新保存的 commit 不必等待异步通知即可生效。键盘与候选栏配置不变时不重刷所有已有图层。
- 回归覆盖 200 次连续读取零额外磁盘同步、写入中断保持关闭的渐变、立即保存生效。QQ 标识的测试宿主强制浅色界面与浅色输入框，检查实际系统键盘的自定义键帽像素、按下/松开及图标；该宿主不是 QQ App。
- 无 iOS 17.0 真机、实际 QQ 或巨魔安装 App 的运行数据，不能确认其所有绘制覆盖和卡顿根因已解决。诊断增加 QQ hook 状态、覆盖计数与存储读取次数；不包含输入内容。

## Samsung13 霓虹键帽轻弹

以下记录最初实现；其中渐变、高光与共用颜色参数已被 samsung15 的纯色方案替代。

- 光效样式新增值 `2`「霓虹键帽 · 轻弹」。默认值仍为 `0`，不自动更改用户模式。只对实际命中的键帽绘制亮面渐变、高光和轻微弹簧动画，不运行背景霓虹、圆环或邻键传播。
- 沿用不透明度、浓淡、亮度、持续时间、同时光效上限和颜色模式。新模式适度提升同一浓淡/不透明度值的显色程度，零值仍有效。传播、柔度、中心强度与背景参数不适用；设置中已注明。
- 视觉回弹使用 Core Animation 弹簧，仅对能匹配的单键视图附加带名称的动画；不改键盘模型坐标或触控区域。找不到独立键视图时只对亮灯表面回弹。清理只移除本模式的动画，保留键盘系统动画；遵守系统减弱动态效果。
- 使用当前键帽范围内的临时字形图，保护亮灯时文字/图标。局部图只在内存中存在，动画结束释放；不保存输入内容或实际键帽图像。未知复杂皮肤无法可靠分离字形时降低覆盖强度，不使用 iOS 不可靠的图层混合滤镜。
- 同键连击替换原动画，切换模式/键位变化/离开窗口清理效果。原生与微信配色、按下态修复、候选词与同步代码未更改。
- `--native-neon-press` 集成检查实际系统键盘截图：单键出现彩色像素、白色字形可见、动画结束恢复原色。另有缩放模型不变、动画清理、减弱动态效果、连击和模式切换回归。仍需 iOS 17.0 真机和实际微信键盘验证。

## Samsung12 设置包主类修复

- 复现：在尚未加载设置包的进程中，用系统 `SpecifiersFromPlist` 解析实际入口，再调用 `PSListController.lazyLoadBundle:`。samsung11 被解析为 `RKBPackageInfoController`，调用 `setSpecifier:` 时抛出 `NSInvalidArgumentException`。新增关于页后，包内不再只有一个控制器，而 `Info.plist` 没有指定 `NSPrincipalClass`。
- 修复：在设置包 `Info.plist` 明确声明 `NSPrincipalClass = RKBRootListController`。不调整类顺序、不删除关于页、不改键盘或配置同步算法。
- 回归：设置测试不再直接用 `NSClassFromString` 创建根控制器，改为从未加载的实际入口解析和懒加载，验证主类、实际详情类及设置基类一致，然后检查 33 个控件、14 项说明、保存和选色。静态与发布校验同时检查最终包内的主类声明。
- samsung11 的列表兼容修订未触及这个入口问题。此前的页面直建测试绕过了系统选择主类的过程，不能作为入口正常的证据。上述异常已在模拟器复现，iOS 17.0 真机结果仍需用户确认。

## Samsung11 设置入口兼容修订

- 将 14 个滑条的分组标题和说明声明在 `RainbowKeyboard.plist` 中。恢复 samsung9 的 `loadSpecifiersFromPlistName:target:` 加载方式，不再加载后重组分组、不再将 `_specifiers` 转成不可变数组。
- 不改变设置键名、默认值、保存与同步代码，不删除用户配置。保留原生颜色选择器、图标、关于与效果预览。
- 静态检查要求每个滑条前有标题组、后有完整说明且不混入其他控件；运行检查补充设置列表可变性、框架插入/删除和反复刷新。
- 无用户崩溃日志，无法确认设备上的具体异常栈；旧版能通过本机 iOS 26.5 模拟器页面测试，不能因此断言 iOS 17.0 真机兼容。本修订移除新增的列表重组路径，须安装后复测。

## Samsung10 设置同步与发布

- 设置保存后发布完整的显示快照，不再只传候选色与键帽色。覆盖 14 个滑条、模式与预设、两个键帽配色、候选词起止色及独立开关。
- 快照带修订号、存在位和完整性校验；读到未完成写入时保留上一份有效值，避免忽然回到默认黑色或重新打开渐变。无效数字交回原渲染器的默认逻辑，不改原有预设。
- SpringBoard 仅从真正保存过的配置恢复快照，启动、配置变化或客户端请求时重发；不以默认值覆盖用户设置。App 启动、回到前台或键盘重新显示时请求同步并刷新已有键面。
- 新增 `Tests/run-preference-relay.sh`：用独立写入/中继和读取进程，验证写入进程退出后恢复、读取端无偏好文件权限时同步、写入中断及通道状态丢失后的恢复。本机模拟器读取端确实无法从存储读到写入端的值。仍需 iOS 17.0 真机确认注入与通知权限。
- 每个滑条独立显示名称和下方说明，共 14 项。说明区分大小值、生效开关、三星/圆环差异，以及纯黑主题下刻意不填亮键面的参数。使用系统 `PSGroupCell` 页脚，不引入自定义滑条 cell。
- 图标、离线关于页、两张实际渲染预览随 PreferenceBundle 安装。`Publishing` 内含 1024px 图标、256px 在线图标、横幅、原生 Sileo JSON/HTML 生成器、完整源索引生成及校验工具。
- 保留原生键帽引擎诊断 samsung9、微信键帽引擎诊断 samsung8；包版本为 samsung10，设置同步协议为 v2，诊断新增配色与修订号。版本差异表示渲染未改，并非仍加载旧包。

安装后必须重新载入界面，让 SpringBoard 加载同步中继，再重新打开设置与输入应用。不要求微信键盘更换皮肤或清空已有配置。

源发布需要真实 HTTPS 地址：工具据此写入 `Icon`、`Depiction`、`SileoDepiction`。未给源地址时只提供基础安装包和离线素材，不伪造可上传链接。已有源不要用仅含本插件的索引覆盖全源索引。详见 `Publishing/README-publishing.md`。

## Samsung9 原生键盘专项

- 微信输入法进程不安装任何新增的原生状态、着色或背景渐变 hook；微信按键遍历、图片算法、候选词、设置值与霓虹绘制不变。微信诊断版本仍为 samsung8，原生诊断为 samsung9。
- 原生按下/松开期间通过 `layerForRenderFlags:` 返回值识别系统实际操作的背景层，不依赖固定图层下标或硬编码 render flag。拦截此前遗漏的 `contentsMultiplyColor`：已着色位图使用白色乘色避免二次变暗；GPU/遮罩背景使用选定键帽颜色。只在确认的背景层阻止合成混色和乘色过渡，保留字符层与移动动画。
- `UIKBRenderFactory` 返回的实际 keyplane 按键背景 traits 使用独立副本，替换背景渐变及按下变体，不改 symbolStyle/前景渐变。为配色生成独立缓存标识，关闭主题返回原始 traits。系统内置色名表不能直接表示任意 RGB，因此仅给本插件创建的原生背景渐变对象提供 CoreGraphics RGB 绘制；保留复制和渐变返回值的所有权约定。
- 复制 traits 时保留系统复制方法遗漏的 geometry / variantGeometries，避免表情等图形键失去符号。状态背景也可能混合字形，继续使用保字形的图片转换，不将整图直接填色。集成测试另对表情键做整窗口合成截图，检查普通、按下、松开时的图标，避免 GPU 字形未出现在单层截图中造成漏检。
- 补上 keyplane 状态切换、替换活动视图、renderer 完成和单键 backdrop 配置后的刷新。单键 backdrop 使用键帽色，而不是整个键盘的底色。
- 原生键面单独处理 16×16 以内、近乎不透明且纯色的拉伸背景图片；要求图层确实覆盖原生键面，排除白色字形和图像遮罩。微信图片转换器保持原样。
- 原生诊断增加 `nativeStateHooks`、`nativeTraitsHook`、`nativeMultiplyHook` 和处理计数。只包含接口状态和数量，不含文字、按键名称或图像。

核对 iOS 17 runtime headers 中的 `UIKBKeyView`、`UIKBKeyplaneView`、`UIKBGradient`、`UIKBRenderTraits` 接口；在本机 iOS 26.5 模拟器进一步检查背景状态切换确实写入乘色和合成滤镜，并验证渐变所有权和实际 RGB 像素。未在 iOS 17.0 真机复现用户截图，不能把模拟器和测试夹具通过当成真机确认。

## Samsung8 修订

- 按下态增加 `UIButton` 各组合状态的背景图处理，保留未指定状态的系统回退行为，关闭主题还原最新原图。仅对确认覆盖键面的图片补充白色、近白和彩色底识别，保留白字；字符遮罩和普通应用图片不处理。
- 补充附着后才设置尺寸、路径、填充色的键面形状处理；触摸移动、松开、取消时也刷新主题，保留粗边框和键缝扩散。
- 设置页去掉自定义 `PSTableCell` 初始化依赖，底色与键帽选色入口改用标准 `PSButtonCell`，色块由控制器提供。已用模拟器的实际 Preferences.framework 加载该控制器、逐行生成设置并打开四个选色器。用户的 iOS 17 闪退未在模拟器复现，此处是兼容性修订，仍需真机验证。
- 配置先持久化后通知，按修订号解决文件、偏好域、跨进程通道的新旧冲突。打开设置页不再发布默认值；写入中的不完整通道不会让无文件权限的扩展默认开启渐变。
- 微信候选标签即使嵌在原生候选容器内，也服从微信开关，不再由嵌套文字 hook 重复染色。关闭时使已绘制的渐变缓存失效，键盘重新显示时重读设置；选色不会重新打开已关闭的渐变。
- 诊断升级为独立 v8 通道，复制报告附带已保存的候选词开关及修订号，不包含输入内容。

本机验证仅覆盖 iOS 26.5 模拟器、系统键盘和微信风格测试夹具，不代表 iOS 17.0 / 微信输入法真机通过。安装后重新载入界面并重开设置与使用键盘的应用，以卸载旧版本 hook。

## Samsung7 按下态与颜色

- 原生按键：在签名检查通过后监听 `changeBackgroundToActiveIfNecessary`、`changeBackgroundToEnabled`、`prepareForDisplay`，按下和松开时重新处理实际背景与字符层。接口依据 `MTACS/iOS-17-Runtime-Headers` 的 `UIKBKeyView.h`，并非根据方法名称盲目调用隐藏键帽。
- 动态图层：已识别按键中新插入、替换的图层和子视图立即纳入处理，不等待下一次布局。只移除已管理键面上的背景、图片、填充颜色过渡，保留缩放等反馈；组合动画和附着之前添加的颜色动画也处理。应用普通图层、候选区和工具栏不在范围内。
- 微信按下图片：`UIImageView.image` 与 `highlightedImage` 分别缓存和还原；高亮图刷新、`UIControl` 高亮/选中状态变化均重新检查。浅灰按下图的识别范围扩大至 245/255，不再仅处理 180/255 以下灰色。
- 设置保留一个入口，将旧“纯黑键帽与底色”改名为“自定义键帽与底色（原生/微信）”，沿用原开关键，新增“键盘底色”“键帽颜色”两个带色块的系统选色器。两者默认 `(0,0,0)`，不清空已有霓虹或候选词设置；按下时沿用键帽颜色。
- 配色单独通过非敏感 Darwin notify 通道传给键盘扩展。换色后从保存的原图重新着色，颜色、形状、渐变、普通/高亮图缓存均失效重算；关闭后恢复最新系统皮肤。只传 RGB 值，不传图片、按键或输入内容。字形仍保留白色，因此推荐选择与白字有对比度的键帽色。
- 首次布局若键位尚未就绪，会在按键准备好后合并刷新独立键帽层，避免自定义色必须点击一次才显示。纯黑字形遮罩不作为灰色键面识别；系统集成检查同时要求普通、按下和松开状态保留白字。
- 诊断使用独立 v7 通道，旧进程报告不会误认为 samsung7。进程诊断文件增加 `stateRefreshes`。

回归增加连续高亮切换、新附着按下层、组合变色动画、浅灰高亮图、独立配色、缓存换色与恢复、跨进程配色和运行中更新。模拟器系统键盘集成检查也调用实际按下/松开背景接口，并检查两个状态的键帽像素。`--native-colors` 另外检查真实系统键位上的自定义配色，不等同于 iOS 17.0 / 微信真机验证。

## Samsung6 诊断修订

用户的 samsung5 报告显示两种键盘均有配置和视图 hook，但图片转换计数为零。这只能说明没有转换成功，不能据此断言所有灰色一定来自直接绘图，也不能将“已安装 hook”当成“已经识别到键帽”。

- 独立原生背景：对 `UIKBKeyplaneView` 中可确认的 `_keyBackgrounds`、`_keyBorders` 表面抑制显示，保留 `_keyCaps` 字符层；系统刷新时继续抑制，关闭后恢复最后一次请求的显隐状态。不使用固定图层下标，不强制调用可能影响字符的 `hideKeyCap:` / `hideKeyCaps:`。
- 半透明图片：灰色检测以解预乘后的颜色判断，保留原始 alpha，补上上一版要求近乎不透明而漏掉的键面。
- 直接绘图：仅对已标记键盘视图的 `drawLayer:inContext:` 捕获当前绘图，处理灰色键面后合成回原上下文，保留白字及抗锯齿。限制图片尺寸，无法识别的内容原样绘制，不保存实际按键图像。
- 形状/渐变背景：只有尺寸覆盖键面且内部采样确认填满的 `CAShapeLayer` 或无遮罩整面 `CAGradientLayer` 才转黑，小字符形状不处理。保留开关恢复和后续颜色更新。
- 工具栏：统一候选栏、dock、accessory、clipboard、shortcut 等排除规则；远程输入容器不再自动被选作光效宿主，避免把地球、复制、撤销等按钮当作键帽。
- 诊断：使用独立 v6 通道，避免旧进程报告被误标为新版本。增加 `directDraws`、`directConversions`、`suppressedBackgrounds`、`shapeFaces`、`observedKeyViews`，分别表示直接绘图、转换成功、背景抑制、形状键面和观察到的按键视图。无内容、图像或键名。

GitHub 调研：检查了 `Paisseon/DarkKeysReborn` 的 `DarkKeysReborn.x`，提交 `289bc44e63c850f4224736d12cbbea22cedc6818`（2021-10-02）。其通过固定图层下标隐藏键帽；本版只借鉴分离背景与字符的思路，没有照搬层级索引，也没有把旧项目当成 iOS 17 兼容性证明。另核对 `MTACS/iOS-17-Runtime-Headers` 中 `UIKBKeyView`、`UIKBKeyplaneView`、`UIKBSplitImageView` 的接口和成员信息。

参考源：
```text
https://github.com/Paisseon/DarkKeysReborn/blob/289bc44e63c850f4224736d12cbbea22cedc6818/DarkKeysReborn.x
https://github.com/MTACS/iOS-17-Runtime-Headers
```

新增回归覆盖直接绘图的纯黑面/白字、25%/50%/75% 透明键帽、语义背景层显隐与恢复、形状/渐变刷新及还原、dock 宿主与键位排除。白字抗锯齿边缘本来就有灰像素，测试单独测量键面，不要求字符边缘变成生硬的纯白。

## Samsung5 兼容性修订

- 前两版只处理单个 `UIKBKeyView` 的缓存，且明确排除了微信键盘。现在原生键盘额外处理 iOS 17 的 `UIKBKeyplaneView` / `UIKBSplitImageView` 整图路径：背景、边界与字符缓存分开处理，不把整张键盘字符一起涂黑。
- 整图缓存支持至 4096×2048、总像素不超过 2,097,152；不再套用单键的 1024×512 上限。字符与键帽混合图按不透明连通区域识别灰色/蓝色键身，分别消除底色，保留字形和 alpha。图片、背景色修改都有缓存与还原。
- 微信键盘在输入法控制器布局和键盘触摸时识别按键视图，处理其背景色、按键图片、普通及富文本字符。图片/颜色刷新后重应用，关闭开关恢复最后一次皮肤值。候选栏、工具栏不作为键帽处理，离开按键树的复用视图不继续染色。未识别的自定义整屏绘制或未知图像格式不会盲目覆盖。
- “纯黑键帽与底色（原生/微信）”沿用原配置键，两个键盘分别服从各自开关。跨进程配置补上微信键盘开关，纯黑模式的键缝遮罩也覆盖微信。
- 设置增加“复制键帽诊断”：只复制构建版本、系统版本、原生/微信最近上报的接口状态与处理计数，不复制输入、候选词或图像。计数上限 255；没有报告表示本版未收到该键盘上报，不等同于已证明无法注入。

本机只有 iOS 26.5 模拟器，没有 iOS 17.0 真机或微信输入法可运行副本。整图路径及微信视图使用专项测试夹具验证，模拟器系统键盘另做集成验证；不能将这些测试声称为 iOS 17.0 / 微信真机通过。安装后分别打开两个键盘输入几次，若仍灰，请在本插件设置中“复制键帽诊断”并反馈。

## 本次修改

- 默认使用键位扩散：周围键框按距离依次发光、退色。纯黑模式不再给按下或相邻键面填色。
- 背景霓虹沿真实键缝扩散。纯黑模式取消键面微光，并对整个光波（包括模糊阴影）使用键帽反向遮罩，连续输入的叠加光效也不会把黑色键面染成紫灰色。关闭纯黑模式仍保留上一版的键面微光。
- 原生键盘的底板和独立不透明键帽层均使用 RGB `(0, 0, 0)`；键帽及背光共享同一几何路径，白色字符保持原生绘制。微信输入法的已识别键帽也启用纯黑处理。视频受曝光影响，不能据此断言三星原版的精确 RGB。
- 修复上一版只处理 `Return-Key` 的遗漏：现在检查原生 `UIKBKeyView` 的字母键、空格键、功能键及其嵌套缓存图层，同时处理占主要面积的灰色底图和零红通道蓝色底图。灰色底图按行估算底色并反解白色字形的混合值，保留抗锯齿、彩色字符及圆角透明度，不把白字一起涂黑。每层只缓存最近一张转换图，关闭主题还原原图；纯字形、未知底色和超过 1024×512 的图像不修改。
- 监听系统专用 `_UIKBKeyViewLayer` 的缓存图赋值，避免后续刷新重新覆盖黑色；补上键盘类延后加载的安装重试，布局、填充和显示接口分别检查签名。UIKit 图片、颜色与字符更新 hook 仅处理已识别并标记的键盘子树，不全局修改应用图片或文字。
- 彩色边框改为键帽外侧的实心渐变环，普通键宽 2.6pt、按下键宽 3pt；不再用会被纯黑遮罩裁掉一半的细描边。背景扩散和颜色浓淡设置保留，键面仍为纯黑。
- 新增“霓虹颜色浓淡（浅→浓）”，范围 0～1，默认 0.72；低值偏浅淡，高值偏浓艳，0 为无彩色白光。与独立的“霓虹亮度（暗→亮）”共同调节，覆盖键框、背景扩散和旧版圆环，不改变纯黑键面与候选词渐变。柔和预设使用 0.4，鲜艳预设使用 1。
- 原生键盘通过 `keyplane.keys` 获取实际几何信息，调用前检查接口签名；第三方键盘从可识别的按键视图获取位置。不硬编码全键盘或九宫格，未识别到键位时不绘制。
- 支持切换回原版圆环。保留颜色、强度、传播时间、范围、同时光效数量等参数。
- 微信原实现是 `WBTextItemLabel` 的候选词内部横向渐变，默认 RGB 起点 `(0, 0.65, 1)`，终点 `(0.85, 0.15, 1)`。
- 原生候选词与微信共用同一渐变函数及颜色设置。除候选区中的 `UILabel`、`NSString` / `NSAttributedString` 外，修复了直接使用 CoreText 绘制的 `TUICandidateLabel`：提取实际字形透明度，按词的实际着墨宽度着色。
- 英文预测栏的 `TUIPredictionViewCell` 原先使用逐字缓存图片的 `UIMorphingLabel`，会绕过文字绘制 hook。开启原生渐变时改用 UIKit 已有的普通标签路径，保留布局和选择行为，但不保留逐字变形动画。
- 渐变仅影响候选词、预测词，不给搜索按钮、键帽字母、应用输入框或聊天消息染色。私有接口先检查签名，支持 TextInputUI 延后加载。
- 候选词渐变在没有历史设置时默认开启；已经明确关闭的设置不会被强制覆盖。

## 设置

只保留 `RainbowKeyboardPrefs` PreferenceBundle 的完整设置入口。旧 simple-plist 入口已删除，它的 8 个有效选项在完整页全部保留，沿用同一个偏好设置域和配置键，不清空已有设置。更新后如设置 App 仍显示缓存的两个入口，关闭设置 App 后重新打开。

主设置页的“光效样式”选择“三星风格键位扩散”，开启背景传播及背景霓虹扩散，可调扩散光强度。“自定义键帽与底色（原生/微信）”控制颜色主题；下面分别选择“键盘底色”“键帽颜色”，两项都选黑色即恢复纯黑。关闭主题后恢复原皮肤。效果预设保留自定义底色与键帽色。候选词渐变需要同时打开“候选词渐变”及“原生候选词”。

安装更新后重新启动使用键盘的应用；切换纯黑主题或首次启用预测词渐变后也应重开应用，以重建系统缓存键帽和预测标签。若旧键盘进程仍驻留，可在安装器中执行重新载入界面。

## 构建

使用已有 Theos 和 iOS SDK：

```sh
THEOS="$HOME/theos" gmake -j4 package ARCHS=arm64 THEOS_PACKAGE_SCHEME=rootless FINALPACKAGE=1
```

RootHide 必须使用 RootHide 版 Theos，选择 `ARCHS=arm64e THEOS_PACKAGE_ARCH=iphoneos-arm64e THEOS_PACKAGE_SCHEME=roothide`；不要直接把 Rootless 包重命名成 RootHide 包。

GitHub Actions（`.github/workflows/build.yml`）已改为纯 Rootless 构建：macOS runner 上安装 Theos + iPhoneOS16.5 SDK，执行 `ARCHS=arm64 THEOS_PACKAGE_SCHEME=rootless make package FINALPACKAGE=1`，产物为 `build/RainbowKeyboard-rootless.deb`（Actions 页面可下载）。

## 验证和边界

```sh
bash Tests/run-simulator.sh BOOTED_SIMULATOR_UDID
bash Tests/run-simulator.sh BOOTED_SIMULATOR_UDID --native-keyboard
bash Tests/run-simulator.sh BOOTED_SIMULATOR_UDID --native-colors
bash Tests/run-simulator.sh BOOTED_SIMULATOR_UDID --settings
bash Tests/run-simulator.sh BOOTED_SIMULATOR_UDID --wetype-isolation
xcrun swift Tests/check-settings.swift .theos/_
```

测试在 iOS 模拟器中运行同一份光效实现和由 Logos internal generator 生成的候选词、纯黑主题 hook。覆盖两端渐变像素、微信/原生一致性、独立开关、普通应用不染色、自绘文字、键位过滤、未知 ABI、连打上限、未知布局、旧模式、扩散延迟、背景光开关与像素、图层回收。

还验证真实系统 `TUICandidateLabel` 的中文渐变及真实 `UIKBBackdropView` 的不透明纯黑像素；检查不透明键帽、重叠光波期间键面保持纯黑、粗边框宽度，以及搜索键缓存去蓝和恢复。灰色回归覆盖三种底色、纵向底色渐变、嵌套图层、白字和抗锯齿、彩色字符、圆角透明度、缓存复用、关闭恢复和原生图层赋值。`--native-keyboard` 打开系统键盘和固定测试词，输出整键盘截图、实际键位上的光效截图 `native-wave.png`、所有实际键帽的像素统计、预测绘制计数和实际键位数。输出在测试 App 的 `Documents` 目录。

这些包含 UIKit 测试夹具和模拟器系统组件验证，但不等同于真实微信输入法或越狱设备验证。不同 iOS 版本和越狱注入机制仍需真机确认；未知接口会跳过，不盲目 hook `renderCandidateWord:focusedStyle:atIndex:`。

原生渐变诊断写入 `/var/mobile/Library/Preferences/RainbowKeyboard-native-probe-<应用标识>.plist`，无权限时回退到进程临时目录。只记录系统版本、绘制类名、接口和开关状态、绘制计数，不记录用户输入或候选词内容。

纯黑诊断写入相同目录的 `RainbowKeyboard-black-probe-<应用标识>.plist`，记录接口安装状态、视图/图层类名及灰色/蓝色图像处理数量，不记录按键名称、输入内容或图像。若真机仍灰，请附此报告及 iOS/键盘类型，以区分未注入、接口差异和其他皮肤插件的绘制覆盖。

真机验收应检查原生拼音全键盘、九宫格、英文预测、展开候选栏、微信输入法、横竖屏、浅色/深色、连续输入和开关恢复。若某个候选区仍是原色，需要提供 iOS 版本、越狱类型、键盘类型及效果录屏，再针对其实际绘制路径适配。
