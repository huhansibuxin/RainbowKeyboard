Package: com.minis.rainbowkeyboard
Name: 彩虹键盘光效
Version: 1.5.0
Architecture: iphoneos-arm64
Description: 霓虹扩散与键帽光效，支持原生键盘和微信输入法
 键缝背光、粗彩色键框、候选词渐变及独立开关，不改动键帽原生配色。
 支持跨 App 配色同步，每个滑条附有作用与调节说明。
 霓虹键帽纯色轻弹：逐次换色，独立调节暗光至强光，无键内混色。
 优化配置重复读取与键盘重刷，增加 QQ 浅色键盘配色兼容。
 注入范围收窄为原生键盘(InputUI)与微信键盘扩展(wxkb_plugin)两个进程。
 改用精确类注册表：按键区、键帽、候选栏由类钩子在布局时登记，
 输入与绘制路径不再递归视图树、也不逐层匹配祖先类名。
 修复 17.3 光效全部失效：显示状态为位置型位域，删除开关项导致后续位号前移，
 现保留占位槽并升级快照版本号，彻底隔离旧版残留状态。
 修复 17.6「自定义」不生效：预设与自定义写的是同一批参数键，预设没有备份就
 覆盖了用户自己的数值，之后选「自定义」只能改标签、参数仍是预设的。现在预设
 生效前先把自定义参数存入 CustomParams，选「自定义」即完整还原。
 17.7 热路径瘦身：CA 动画的常量数组（keyTimes/locations）改为共享，不再每次
 按键重建；键缝遮罩的复合路径改为按 bounds 缓存。观感不变，按键时对象分配
 减少约 43%。
 17.8 新增「自用」预设：把当前这套参数固化成预设，列在柔和水波之前。它使用
 新编号 4，原有四个预设的编号不变，已保存的预设选择不会被改义。渲染层的出厂
 回落值改为引用同一张参数表，因此键盘进程读不到配置或全新安装时也呈现这套
 观感，不再落回旧的推荐值。
 17.9 修复九键切全键盘后扩散原点仍留在九键区域：键位表过去只在键盘区尺寸
 变化时重建，而换键位布局并不改变该尺寸，于是光效一直沿用旧键位。现改用
 「布局代际 + 注册键数 + 尺寸」判断，换布局立即重建、连续打字不重建。同时
 淘汰被隐藏容器收纳的旧键帽（其自身可见性仍为正常），避免旧键混入。
 17.10 修复候选词变色会被「自定义」和回落状态自行打开：候选开关过去是
 「键不存在即视为开启」，而这些状态恰好都不写这三个键，于是自行变成开启。
 现改为「必须显式开启」；设置页三个候选开关的默认值同步改为关闭，显示与
 实际生效不再矛盾。主开关另做缓存，关闭时每次文本绘制只读一个标志位。
 键盘扩散全部功能不受影响。
 1.2.0 在 17.10 基板之上做热路径与几何判定的收尾，观感与开关行为一律不变：
 一、配置改为推送式。设置页保存时通知键盘进程重新解析，按键路径不再每次都取锁
 并做一次跨进程查询，只剩一次浮点比较；守护窗口每秒最多重查一次，兜住键盘扩展
 被挂起期间漏掉的通知。
 二、按键路径的参数取值一次解析完毕。原先每次按键要在两张表里做约四十二次取值
 （含类型探测与拆箱），现改为配置变更时解析成扁平数值，绘制路径直接读数值。
 三、键缝遮罩层改为复用。复合路径本来就已缓存，但包住它的形状层仍是每次按键新建。
 四、注册表改为直接枚举，不再经 allObjects 每次都分配一个数组。
 五、轻弹模式的键帽轮廓提取改为可复用：只有重新渲染出的位图与上次逐字节相同时
 才复用（因此大小写切换、中英切换都不会取到过期的字形），同时逐像素提取只处理
 能带墨的像素，键面本身被跳过。
 1.2.1 回退上述第四条：布局代际恢复为宿主每次布局都前移。收窄后实机在微信键盘上
 复现了 17.9——九键切英文再切回九键，键位表仍停在英文那套（两套键面的键帽在切换
 时都已登记，切回九键既不新登记键帽、注册数也不变，收窄后的两个判据同时不动）。
 连续打字不必重算键位表由「门」负责：代际、注册数、尺寸三者都没动才复用。
 1.3.0 扩散光效的图层改为复用（诊断构建，含内置度量，日志 rkperf.log）：
 一、扩散光效每次按键过去要现建约四十个图层——一个容器、两层环境光底与两个径向
 光晕、以及每个亮起的键四个图层（键体、轮廓光、渐变、裁切环），每个键还要现算
 贝塞尔路径再拷进两个形状层。而这些几何与按键无关：键的外框、裁切环、各层 frame
 只取决于该键的矩形，键盘不换键位就不会变。现改为按「脉冲 × 键」建一次并保留，
 按键只写真正变化的值（色相、每键透明度峰值、光晕圆心、动画起始时刻），动画对象
 也保留复用。因此打字过程中不再向图层树增删任何图层。
 二、脉冲与效果上限同容量（默认二），重叠按键的可见波数与原版一致：池未满时新按键
 独占一个脉冲，池满时复用最先结束的那个——即原版会丢弃的那个，但它继续参与绘制。
 三、键位表、观感尺寸、效果样式或功能关闭时释放整池，之后首次按键重建（均为用户
 节奏的操作，不在打字路径上）。
 四、新增诊断文件 RKProbe.h：每次按键记录冷/热分类、点亮键数、新建与复用组数、
 新建图层与路径数、装配耗时与整条按键路径耗时，另每若干次汇总一行平均值。冷按键
 （建有新组，付的是旧开销）与热按键的平均值并列，可直接对照出节省量。日志写在
 键盘扩展自身沙盒的临时目录，RK_PROBE_ENABLED 置 0 即整段移除、不留痕迹。
 五、如实更正 1.2.0 第三条的描述：键缝遮罩层的两个调用点都被「是否保留黑色键面」
 把门，而该判断是常量否，所以那一层从未被创建过，该项实际收益为零；它是死代码，
 不是每次按键的开销。本条只是更正说明，不改变任何行为。
 六、轻弹键帽轮廓复用与配置推送式等同 1.2.0，未变动。
 1.3.1 修正 1.3.0 诊断日志的落盘位置（仍为诊断构建，行为与 1.3.0 相同）：
 一、日志路径改为运行时解析。键盘扩展只被授予自己的容器，且容器 UUID 每次安装或
 重启都可能变化，写死路径必然落空。现依次尝试扩展容器内的若干位置：环境变量
 TMPDIR、容器 Documents、容器 Library/Caches、容器 tmp，最后才是系统临时目录；
 逐个以追加方式打开，取第一个能写的，整轮沿用同一路径，因此同一次运行的记录都
 落在同一个文件里。
 二、新增启动自检行。文件首行在 dylib 载入时即写入，内容为该进程号与实际采用的
 日志路径。按键记录只有在一次按键真正走到扩散光效绘制路径之后才可能出现，单凭
 它无法区分「插件没进这个进程」与「进了但没触发」；有自检行说明代码确实在进程里，
 有自检行而无按键行说明加载成功但按键路径没走到。自检行在日志轮转后会自动补回。
 1.4.0 修掉「光斑跟着按键滑行」的来源、把跟随滑行做成开关，并做同批收尾
 （仍为诊断构建，日志同 1.3.1）：
 一、新增「背景霓虹跟随滑行」（默认开启）。开启时背景光晕的光斑从上一个按键的位置
 滑到当前按键，连续打字像一块光跟着手指走；关闭时直接出现在当前按键上、不发生位移，
 与更早版本观感一致。只影响背景霓虹的光斑，不影响键框与键面本身的亮灯。
 二、滑行的来源：1.3.0 起图层改为复用，而 Core Animation 对已在图层树中的图层，
 每次写属性都会自带一段 0.25 秒隐式动画。旧版每次按键都新建图层，赋值时没有旧值，
 所以从不出现位移；复用之后光晕圆心每次都被改写，于是从上一次按键的位置滑过来。
 现对池内全部图层按属性关闭隐式动画，跟随滑行改用显式动画实现，时长与缓动曲线
 与原隐式一致，观感不变。这同时省掉每次按键数十次隐式动画对象的创建与事务记账，
 是这一版最大的一笔开销削减。
 三、颜色瘦身：alpha 变体改由同一个 CGColor 直接拷贝（原先每个变体都要新建一个
 UIColor 再取其 CGColor），每按键少建六个颜色对象；背景光晕的两个光斑共用一组基色，
 不再各算一套；渐变键框的两个色标改复用同一数组。
 四、键框轮廓补上阴影轮廓路径（shadowPath）。原先没有轮廓路径，可见期间每帧都要
 离屏重绘整层，属耗电项，与主线程耗时无关。
 五、只与几何或配置有关的写入移出按键路径：描边宽度随键面变体在建组时写入，
 阴影模糊半径随配置写入，动画的数值表、关键帧时刻与时长在此时一次设定，按键只写
 起始时刻。键位表改为平坦数值数组，两遍扫描不再逐个拆箱，按下键位改用下标比较。
 六、配置真正改变时才释放图层池（每秒一次的兜底重查不再导致池被清空，
 否则每秒钟的第一次按键都会退化成冷按键）。
 七、修正 1.3.0 探针汇总行的单位（漏乘 1e6，此前平均值恒显示 0.0），并为
 「颜色」与「动画提交」两段增加分段计时，便于逐项核对节省量。
 1.4.1 修复 1.4.0 新增的「背景霓虹跟随滑行」开关关掉后仍然滑行
 （仍为诊断构建，日志同 1.3.1）：
 一、原因是这个键没有登记进跨进程快照。键盘扩展受沙盒限制，读不到偏好文件，
 也读不到其它域的偏好，渲染参数实际全部经由快照从设置进程传到键盘进程，而快照是
 位置型位域：每个键占固定的存在位与数值位，不登记就等于不存在。新键漏登记时，
 键盘进程读到的是「该键未设置」，而开关的语义是「未设置即开启」，于是不论设置页
 关成什么，键盘进程都按开启渲染，现象就是关掉后照旧滑行。
 二、现为该键分配独立的位对（存在位 44、数值位 45），并登记进由快照提供的键清单，
 使旧值能被新快照覆盖。位置型位域的两段（存在位 21 至 30、数值位 31 至 40）已经
 首尾相接占满，第十一个键无处安放，因此新增键一律使用 44 位以上的成对空位，
 已启用的位号不得重排或回收。
 三、旧快照里这两位为 0，含义是「未设置」，不会被误读为关闭，因此升级后不需要
 清除任何残留状态。
 四、内嵌探针的按键行新增 glide= 字段，记录渲染端实际解析到的值（而不是设置页
 保存的值）。这两个值在键漏登记时恰好相反，可直接用来验证跨进程传递是否成立。
 五、设置页保存时才会重新发布快照，因此升级后需要进设置页再触发一次保存
 （切换任意一项即可），键盘进程随后读到的是修正后的值。
 1.4.2 光效覆盖候选词栏（仍为诊断构建，日志同 1.3.1）：
 一、原先光效视图一律挂在按键面板容器上，而候选词栏是键区之外的另一个视图，因此背景
 霓虹的光晕到了键区顶边就停住，键盘最上边那一行始终没有光。键盘自己的视图树里本来就有
 两个可挂的位置：按键面板容器（只有键区）与键盘主体（同时含顶部候选栏与按键面板）。
 现改为挂到键盘主体上，占用范围即整个键盘，光晕因此可以一直铺到候选词栏。键框与键面
 亮灯仍只出现在键位上。
 二、宿主由精确类钩子登记（微信键盘为 WBMainInputView），与按键区、键帽、候选容器同属
 一张注册表：只做一次弱引用读取，不爬 superview 祖先链、不递归子树。
 三、原生键盘没有登记键盘主体，因此仍挂在按键区、占用范围与之前完全一致，行为不变。
 四、宿主在使用前重新校验：须在屏、须确实包含键区、尺寸比例须合理。任一不满足即退回
 按键区，不会把光效挂到已经换掉的那个键盘上、也不会拖到屏幕别处。
 五、候选词栏的触摸不触发光效：触摸点仍必须落在键区内，点候选词不会点亮任何键。
 六、键位表在键区坐标系里采集，挂到键盘主体后随之换算到光效视图自己的坐标系，并把
 挂载点与占用范围一起纳入变更判定，因此键盘主体出现或变化时光效会自动跟随，不必重启键盘。
 1.5.0 首个正式版：移除全部诊断代码，行为与 1.4.2 一致：
 一、删除性能诊断探针 RKProbe.h 及其在绘制路径上的每一个调用点：按键的冷热分类、
 图层与路径计数、颜色段与动画提交段的分段计时、跨进程开关值的记录，以及写日志、
 启动自检、日志轮转的全部机制。正式版不再向磁盘写入任何文件，也不再为度量读取时间。
 二、随之删除只为记录而存在的计数结构与函数参数。绘制路径因此少一层间接调用。
 三、观感、开关行为、内存与耗时与 1.4.2 完全相同：探针此前已按条件编译关闭、本就不
 参与绘制，本次是把代码本体一并摘除，使源码与产物都不再含诊断痕迹。
 四、1.3.0 至 1.4.2 各版说明中提到的「诊断构建」与日志文件仅适用于当时版本，自本版
 起不再产生任何日志。若设备上曾留下 rkperf.log，可自行删除（在键盘扩展自身沙盒内）。
Maintainer: MoWang
Author: MoWang
Section: Tweaks
Depends: mobilesubstrate, preferenceloader
